# DSP Backend .env.example
# Copy to .env and override as needed. These defaults are development-safe.

# SQLite DB path (container-local). For Rancher/Docker, mount a volume at dsp_db/ if you need persistence.
DB_FILE=dsp_db/dsp.db

# JWT settings (use strong secrets in production!)
JWT_SECRET=dev-only-change-me
JWT_ALG=HS256
JWT_EXPIRES_MIN=120

# Internal DSP service base URL (proxied by /dsp/query)
DSP_INTERNAL_BASE=http://10.45.30.64
DSP_TIMEOUT_SEC=30

# CORS allowed origins (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:3000

# Notes:
# - The FastAPI app should bind to 0.0.0.0 in containers (done by uvicorn start config).
# - DB_FILE path is container-local by default; mount a volume to persist.
